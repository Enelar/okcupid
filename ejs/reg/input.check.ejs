<%
this.chain(function()
{
  var that = this;

  that.first().on('check', function()
  {
    var val = $(this).val();
    phoxy.ApiRequest(['reg/check', __this.name, val], function(r)
    {
      var method = r.data.check ? "removeClass" : "addClass";
      that.first()[method]("error");
    })
  })
})
%>